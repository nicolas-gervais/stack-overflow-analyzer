services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - analyzer-data:/data
    restart: unless-stopped

volumes:
  analyzer-data:
